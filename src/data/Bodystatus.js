export function bodyStatus(endpoint){
    
    if(endpoint.body === ""){

        const payload = { body : body };

        return payload;
    
    }
    else{
        var body = endpoint.body;

        const payload = { body : body };

        return payload;
    }
    
}